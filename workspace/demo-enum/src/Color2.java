public enum Color2 {
  // enum values (objects)
  RED, BLUE; // create 2 objects => like to static final

  // Normal class
  // attributes (can be no attributes)
  // constructors
  // methods

  // an empty constructor is created by default

  // THE MEANING OF ENUMS
  // JVM will help create the Color2 Object (RED, BLUE)
  // As user, you cannot create the enum object by yourself
}
